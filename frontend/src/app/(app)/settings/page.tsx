import { PageShell } from "@/components/shared/page-shell";

export default function Page() {
  return (
    <PageShell
      title="Settings"
      description="Authenticated starter page for settings."
    />
  );
}
